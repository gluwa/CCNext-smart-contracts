[profile.default]
src = "contracts"
out = "out"
libs = ["node_modules"]
solc_version = "0.8.30"
optimizer = true
optimizer_runs = 200
via_ir = true
evm_version = "shanghai"

remappings = [
    "@openzeppelin/=node_modules/@openzeppelin/",
    "@gluwa/=node_modules/@gluwa/"
]
